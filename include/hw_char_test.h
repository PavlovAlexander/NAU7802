#pragma once

// Forward declaration — не тянем тяжёлый заголовок библиотеки в каждый TU
class NAU7802;

/**
 * @brief Запустить полный тест характеризации железа NAU7802 + тензодатчик.
 *
 * Фазы:
 *   0    — заголовок, дамп регистров, выбор стенда (S/O)
 *   0.5  — 4-точечная калибровка 0/10/20/30 г с циклом retry (A/R)
 *   1    — параметрический свип gain×SPS×chopper (32 комбинации), 0 г
 *   2    — drift-тест 4 конфигураций по 2 мин (A/B/C/D)
 *   3    — интерактивный свип нагрузок 0→10→20→30→0 г в best-config
 *   4    — восстановление регистров, итоговая строка DONE
 *
 * Всё взаимодействие через Serial. Запускается нажатием клавиши 'M' из loop().
 * Работает напрямую с NAU7802, в обход accuracy-pipeline — измеряет железо.
 */
void runHwCharTest(NAU7802& scale);
